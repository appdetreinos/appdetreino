export default async function TrainerDashboard() {
  // CÓDIGO ZERO: Apenas texto puro. 
  // Se isso crashar, o problema é a Vercel ou o Next.js Build.
  return (
    <div className="p-10 text-white bg-black min-h-screen">
      <h1 className="text-5xl font-bold">SISTEMA ATIVO</h1>
      <p className="mt-4">Se você está vendo isso, o código do site está OK.</p>
    </div>
  );
}
